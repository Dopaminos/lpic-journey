
Сделайте эти задания, представляя терминал в уме. 
В основном, это теория
# Inode vs Filename 
```
touch testfile
ls -li testfile
mv testfile renamed
ls -li renamed`
```
Изменится ли нода? Почему?

# Hard linking

```
echo hello > file
ln file hard
ls -li
rm file
cat hard
```
Почему файл всё ещё существует?

# Link Count

```
touch a
ln a b
ln a c

ls -li
rm (какой-нибудь из них)
```
Как изменился Link Count

# Inode vs Disk Usame 

```
mkdir testdir
for i in {1..10000}; do touch testdir/$i; done
du -sh testdir
df -i
```
Что выросло сильнее, кол-во нод или занятое место?


# Задачка со *звёздочкой*
*тут уже нужен терминал, задание прям по материалу*
 
Проверь 
```
df -h
du -sh
```

Создай файл
```
dd if=/dev/zero of=bigfile bs=1M count=200
```

Открой `tail -f bigfile` и удали файл после.
Снова проверь df+du. 

Где disk usage, Лебовски? 