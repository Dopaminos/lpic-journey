# Просмотр namespace-ов

1: Запустить `lsns` и изучить вывод команды. 
Какой namespace используется чаще всего?


# Создание namespace-ов

1: Создать PID namespace, внутри него посмотреть, сколько процессов видно и какой PID у родительского (инит) процесса

2: Проверить ps aux в другом терминале и проверить видимость процессов

3: Попробовать изменить хостнейм внутри namespace-а

# Просмотр CGroups

1: Посмотреть группы и контроллеры корневой контрольной группы
2: Посмотреть группу случайного процесса
3: Посмотреть дерево CGroups и определить что в каком слайсе.
4: Посмотреть top CGroups и проверить, что потребляет больше всего ресурсов

# Создание CGroups

1: C помощью systemd, создать ограниченный по ресурсам и скоупу процесс,
посмотреть где, он, что он, нагрузку... и убить.