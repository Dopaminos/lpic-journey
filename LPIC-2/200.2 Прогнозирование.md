## честно, я бы лучше изучил прометеус)) 
## этим и займусь отдельно

collectd - демон сбора данных

основной конфиг: `/etc/collectd.conf`
включение плагинов: секции `LoadPlugin ...`
часто используемые плагины: `cpu`, `memory`, `load`, `df`, `disk`, `interface`, `processes`

оповещения могут приходить с помощью email, desktop приложения и, например, Nagios (визуализатор мониторинга).

rrdtool - round robin database tool, набор утилит для работы с rrd-бдхами


настройки апача находятся в /etc/httpd/conf/httpd.conf
коллектд находится там же, но collectd.conf

ничего интересного...
