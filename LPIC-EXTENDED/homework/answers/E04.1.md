Сделайте эти задания, представляя терминал в уме. 
В основном, это теория
___
# Inode vs Filename 

Нода не изменится, т.к. она не хранит в себе имя файла.

# Hard linking

В LPIC-1 уже был пример того, как ломаются хард линки.

Файл ссылки (вместе с её контентом) существует, т.к. при удалении (или недоступности ссылкой) файла, inode сохраняется, и кол-во ссылок > 0

# Link Count

Кол-во ссылок изменится с трёх до двух. 
Здесь жесткая линковка, поэтому у одной inode есть три имени.

Удаляем любой файл и уменьшаем счётчик ссылок на единицу.

# Inode vs Disk Usame 

Ноды. 
Файлы же всё равно пустые, места не займут. А кол-во нод поменьше будет)

# Задачка со *звёздочкой*
*тут уже нужен терминал, задание прям по материалу*
 
`lsof | grep deleted`
`taskkill <pid который дал лсоф>`